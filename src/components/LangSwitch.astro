---
import { getRelativeLocaleUrl } from 'astro:i18n';
import { locales, parsePath, useTranslations } from '../i18n/ui';

/**
 * ▶ JA / EN の2択。ドロップダウンにしない。
 * path はロケール接頭辞を除いたものなので、切り替えても同じページに留まる。
 */
const { locale, path } = parsePath(Astro.url);
const t = useTranslations(locale);
---

<nav class="lang marker-group" aria-label={t('a11y.langSwitch')}>
  {
    locales.map((code, index) => (
      <>
        {index > 0 && <span class="lang__sep" aria-hidden="true">/</span>}
        <a
          href={getRelativeLocaleUrl(code, path)}
          class:list={['lang__link', 'marker-item', { 'lang__link--current': code === locale }]}
          lang={code}
          hreflang={code}
          aria-current={code === locale ? 'true' : undefined}
        >
          <span class="lang__marker marker" aria-hidden="true">▶</span>{t(`lang.${code}`)}
        </a>
      </>
    ))
  }
</nav>

<style>
  .lang {
    display: inline-flex;
    align-items: baseline;
    font-size: var(--fs-small);
    letter-spacing: 0.06em;
  }

  .lang__sep {
    color: var(--muted);
  }

  .lang__link {
    display: inline-flex;
    align-items: baseline;
    gap: 0.2em;
    padding: 0.1em 0.35em;
    color: var(--muted);
    text-decoration: none;
    transition: color 120ms linear;
  }

  /* 現在の言語は文字色で示す。▶ は base.css が aria-current で出す */
  .lang__link--current {
    color: var(--gold-bright);
  }
</style>
